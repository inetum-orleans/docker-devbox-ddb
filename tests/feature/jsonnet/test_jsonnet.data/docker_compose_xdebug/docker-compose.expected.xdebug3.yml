version: '3.7'
services:
  php:
    environment:
      XDEBUG_MODE: debug
      XDEBUG_SESSION: xdebug3
      XDEBUG_CONFIG: client_host=%docker.debug.host% client_port=9000
    image: php:7.4-fpm
    init: true
    restart: "no"
    user: %uid%:%gid%

networks: { }
volumes: { }