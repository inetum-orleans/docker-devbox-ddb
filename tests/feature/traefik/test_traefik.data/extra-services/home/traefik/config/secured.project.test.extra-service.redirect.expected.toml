# This configuration file has been automatically generated by ddb
[http.routers]
  [http.routers.extra-service-redirect]
    rule = "Host(`secured.project.test`)"
    entrypoints = ["http"]
    service = "extra-service-redirect"
    middlewares = ["extra-service-redirect-redirect-to-https"]
  [http.routers.extra-service-redirect-tls]
    rule = "Host(`secured.project.test`)"
    entrypoints = ["https"]
    tls = true
    service = "extra-service-redirect"

[http.middlewares]
  [http.middlewares.extra-service-redirect-redirect-to-https.redirectScheme]
    scheme = "https"

[http.services]
  [http.services.extra-service-redirect]
    [http.services.extra-service-redirect.loadBalancer]
      [[http.services.extra-service-redirect.loadBalancer.servers]]
        url = "http://192.168.240.211:9999"
